/**
 * Historical reference only.
 *
 * TradeHub Stage 05 moved the Firebase web config into `.env.local` and
 * `NEXT_PUBLIC_FIREBASE_*` variables. Do not import this file into app code.
 */

export {};
